# Resend API key — used by the contact form Server Action.
# Get one at https://resend.com — free tier is 3,000 emails/month.
#
# Local dev: copy this file to .env.local and paste your key.
# Production: set this as an env var in Vercel (Settings → Environment Variables).
RESEND_API_KEY=
